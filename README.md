# iag-packages

Shared `@iag/*` libraries for the IAG platform (events, messaging, auth-client, service-core, â€¦).

Published to GitHub Packages when ready. Consumed via `workspace:*` in the meta-repo until registry cutover.
